/* function count(arr) {
	let postive = 0;
	let negative = 0;
	for (let i = 0; i < arr.length; i++) {
		if (arr[i] > 0) {
			postive++;
		} else if (arr[i] < 0) {
			negative++;
		}
	}
	if (postive > negative) return `postive: ${postive}`;
	else return `negative: ${negative} `;

	//return [negative, postive];
}

console.log(count([-2, -1, 1, 1, 3, 2, 4, 6]));
 */
const a = 1;
let b = a.toString;
console.log(b);
