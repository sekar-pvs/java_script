var input = [[8, 4]];
var input2 = [[8, 4]];
var temp = input + input2;
console.log(temp);

/* for (i = 0; i < input.length; i++) {
	for (j = 0; j < input.length; j++) {
		console.log(input[i][j]);
	}
}

for (i = 0; i < input.length; i++) {
	for (j = 0; j < input.length; j++) {
		if (input[j] < input[i]) {
			input[i] = input[j];
			input[j] = temp;
			temp = input[i];
		}
	}
	//console.log(input[i][j]);
} */
